@page "/login"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect to the properly configured Identity login
        Navigation.NavigateTo("/Account/Login", forceLoad: true);
    }
}